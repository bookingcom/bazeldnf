load("@bazeldnf//:def.bzl", "bazeldnf")
load("@gazelle//:def.bzl", "gazelle")

bazeldnf(
    name = "bazeldnf",
    use_prebuilt = False,
)

gazelle(name = "gazelle")

load("@centos7-rpm//:rpms.bzl", "RPMS")
load("@bazeldnf//:deps.bzl", "rpmtree")

rpmtree(
    name = "rpms",
    rpms = RPMS
)
